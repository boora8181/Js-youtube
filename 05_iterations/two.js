// while

let index = 0
while (index <= 10 ) {
    // console.log(`value of index is ${index}`);
    index = index+ 2
}

let myarray = [ 'flash', 'batman',  'superman', 'spiderman', 'shaktiman', 'chotaBheem', 'ironman', 'captainamerica' ]

arr = 0
while (arr < myarray.length) {
    // console.log(`your man is ${myarray[arr]}`);
    arr = arr + 1
}

//  do while loop

let score =1
do {
    console.log(`your score is ${score}`);
    score ++
} while (score <=10 );